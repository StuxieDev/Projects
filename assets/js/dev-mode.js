// Production default. dev-server.js serves an overridden version of this
// file (DEV_MODE = true) for local runs, so this committed copy only ever
// ships as `false` — see dev-server.js for how the override works.
window.DEV_MODE = false;
